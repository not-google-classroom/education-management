{
  "urls": [
    {
      "path": "/api/auth/login",
      "method": "POST",
      "roles": ["all"],
      "template": "loginTemplate"
    }
  ],
  "templates": [
    {
      "name": "loginTemplate",
      "minLength": 20,
      "maxLength": 4000,
      "type": "JSONObject",
      "params": [
        {
          "name": "userEmail",
          "type": "string",
          "pattern": "[a-z0-9._%+-]+@[a-z0-9.-]+\\.[a-z]{2,4}$",
          "required": true
        },
        {
          "name": "password",
          "type": "string",
          "pattern": "^(?=.*[A-Z])(?=.*\\d)(?=.*[@$!%*?&])[A-Za-z\\d@$!%*?&]{6,15}$",
          "required": true
        }
      ]
    }
  ]
}